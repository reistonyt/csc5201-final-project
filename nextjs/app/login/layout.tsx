export const metadata = {
    title: 'NewsFeed AI - Login',
    description: 'NewsFeed AI',
  }

  export default function Layout ({ children }: { children: React.ReactNode }) {
    return (
        <div>
            {children}
        </div>
    )
}